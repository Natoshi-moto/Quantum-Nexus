# OS / editor noise
.DS_Store
Thumbs.db

# Local-only working files — never repo-ready by default
/local-dump/
*.local.*

# PC Codex local-only control state
/.codex/NO_LEAK_VAULT
/.codex/NO_LEAK_VAULT.*
!/.codex/NO_LEAK_VAULT.example
/.codex/local-runs/

# Claude Code local-only state
/.claude/settings.local.json
/.claude/worktrees/
