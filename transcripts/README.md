# transcripts/

Index of every session/round that touched this project — chat-based and CLI — with a link where one exists. CLI sessions can't be linked the way chat shares can; those get logged by round number and a handoff-note reference instead of a URL.

**Not yet imported.**
