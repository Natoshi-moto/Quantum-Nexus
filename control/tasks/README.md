# PC Codex tasks

Task envelopes written by the ChatGPT-side controller. One file equals one bounded PC run. A task must name its objective, writable paths, forbidden actions, acceptance checks, and required receipt. Tasks are historical inputs after execution and are not silently rewritten.
