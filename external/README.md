# external/

Third-party code this project depends on or references (e.g. OpenUI), kept clearly separate from original Nexus work — as pointers to upstream where possible, not full vendored copies, unless a deliberate fork is decided on. See `SYNC_LEDGER.md` open question #3.

**Not yet imported.**
